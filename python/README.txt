NDN Lighting v2 - Python

First step in lighting is the configuration manager that authorizes runtime control applications for fixtures.

as of writing, a configuration manager that matches 1-3 of psuedocodev4.pdf is complete.

usage:

> python cm.py sequencer_cfg


note:

though required elements are emerging, config files still in flux.
key files created via command line.

path _/ contains scratch work.